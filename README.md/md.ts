# TypeScript Backend

Este projeto é um backend construído em TypeScript utilizando a arquitetura MVC e conexão com banco de dados relacional (PostgreSQL ou MySQL). O sistema implementa autenticação via JWT e operações CRUD para usuários.

## Estrutura de Pastas

- `src/controllers`: Controladores responsáveis por gerenciar as requisições.
- `src/routes`: Define as rotas da aplicação.
- `src/models`: Modelos de dados (representa as entidades do banco de dados).
- `src/repositories`: Interação com o banco de dados.
- `src/services`: Lógica de negócios.
- `src/utils`: Funções auxiliares como manipulação de JWT.
- `src/config`: Configuração do banco de dados e variáveis de ambiente.

## Instalação

1. Clone o repositório:
   ```bash
   git clone https://github.com/usuario/repo.git
